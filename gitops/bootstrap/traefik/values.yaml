# Traefik Helm values
# Chart: traefik/traefik
# Repo: https://traefik.github.io/charts
# Tested version: 39.0.2

service:
  type: LoadBalancer

# Traefik Dashboard は外部公開しない
ingressRoute:
  dashboard:
    enabled: false

# ログ設定
logs:
  general:
    level: INFO
  access:
    enabled: true
